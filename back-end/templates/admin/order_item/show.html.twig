{% extends 'base.html.twig' %}

{% block title %}Elèment de commande{% endblock %}

{% block body %}
    <h1>Elèment de commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_item.id }}</td>
            </tr>
            <tr>
                <th>Quantité</th>
                <td>{{ order_item.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_order_item_index') }}">Revenir à la liste</a>

  

    {{ include('admin/order_item/_delete_form.html.twig') }}
{% endblock %}
