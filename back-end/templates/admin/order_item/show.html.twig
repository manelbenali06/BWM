{% extends 'base.html.twig' %}

{% block title %}OrderItem{% endblock %}

{% block body %}
    <h1>OrderItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_item.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ order_item.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_order_item_index') }}">back to list</a>

  

    {{ include('admin/order_item/_delete_form.html.twig') }}
{% endblock %}
